{
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "prestart": "rimraf dist && cp \"config.$CONFIG.json\" config.json",
    "prebuild": "rimraf dist && cp \"config.$CONFIG.json\" config.json"
  },
  "browserslist": [
    "last 1 chrome versions"
  ],
  "devDependencies": {
    "parcel": "^1.12.4",
    "parcel-plugin-svelte": "^4.0.4",
    "rimraf": "^2.6.3",
    "svelte": "^3.14.1"
  },
  "dependencies": {
    "lodash": "^4.17.15"
  }
}
